开坑日记：2013-08-13
重构之前写的批量查询单词小程序(命令行)，该小程序原始版本为Old_CLI_Version文件夹里的chadanci.py。
这个小程序实现功能很简单：将指定单词列表中的英语单词(以行分割)的释义查询出来，输出到build/文件夹下一个txt文件中。
前几天发现自己写的程序基本运行在命令行下，GUI方面的技能非常不足，因此决定学习Python的GUI，目前在学Tkinter，这个小程序桌面程序化权当练手了。
Old_CLI_Version文件夹下的beidanci.py程序是基于查单词程序的衍生版本，用于背单词(也是一个无聊的东东)，将HTML文本的转化为正常字符(一点技术含量都没有)，本质上还是查单词程序。
其他的就不说了，说多了就暴露出我无知了。
重构之后的程序在GUI里，myword.py将是程序入口，每个文件夹都是一个包，程序结构如下：

myword.py # 程序入口
enity # 实体
    QQWordMeaning # 从QQ辞典接口查询到的单词释义的实体类(含解析)
model # 模块
    QQDICT # 向QQ辞典接口发起查询
util # 一些工具方法
    html # 将HTML转化为正常符号
view # 视图包
    App # 主应用
